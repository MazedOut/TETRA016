/**
 * Single exception ticket card: type, risk/confidence, status, link to source doc.
 */
export default function TicketCard() {
  return <div className="text-gray-400 text-sm">TicketCard — scaffold placeholder.</div>;
}
