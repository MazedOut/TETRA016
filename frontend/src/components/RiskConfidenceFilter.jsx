/**
 * Dual-scale filter — risk score and confidence score as separate, independently filterable axes.
 */
export default function RiskConfidenceFilter() {
  return <div className="text-gray-400 text-sm">RiskConfidenceFilter — scaffold placeholder.</div>;
}
