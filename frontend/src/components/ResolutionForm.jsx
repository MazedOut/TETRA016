/**
 * Ticket resolution form — mandatory reason, never a silent close.
 */
export default function ResolutionForm() {
  return <div className="text-gray-400 text-sm">ResolutionForm — scaffold placeholder.</div>;
}
