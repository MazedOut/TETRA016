/**
 * AI-proposed ticket merge with editable reason; user accepts, edits, or rejects.
 */
export default function MergeConfirmModal() {
  return <div className="text-gray-400 text-sm">MergeConfirmModal — scaffold placeholder.</div>;
}
