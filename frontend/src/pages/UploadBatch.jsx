/**
 * Bulk upload + invalid-file decision form (skip / retry / review).
 */
export default function UploadBatch() {
  return (
    <div className="p-6">
      <h1 className="text-xl font-semibold">UploadBatch</h1>
      <p className="text-gray-500">Scaffold placeholder — not yet implemented.</p>
    </div>
  );
}
